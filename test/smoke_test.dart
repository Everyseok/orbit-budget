import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke: app constants are defined', () {
    // Populated in Stage 2 with real widget tests.
    // This file exists only so `flutter test` passes in Stage 1.
    expect(true, isTrue);
  });
}
